{
  "name": "yt-metadata-pro-extension",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "clean": "rm -rf dist",
    "validate:extension": "node scripts/validate-extension-package.mjs",
    "lint": "npm run validate:extension",
    "zip": "npm run validate:extension && rm -rf dist && mkdir -p dist && cd extension && zip -r ../dist/YT_Metadata_Pro_Extension.zip ."
  }
}
